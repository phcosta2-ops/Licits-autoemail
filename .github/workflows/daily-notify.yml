name: 🔔 LicitaControl — Notificação Diária

on:
  schedule:
    # Roda todo dia às 11:00 UTC = 08:00 horário de Brasília (BRT = UTC-3)
    - cron: '0 11 * * *'
  # Permite rodar manualmente pelo painel do GitHub
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    name: Verificar e notificar vencimentos

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔔 Executar notificações
        env:
          PIPEDRIVE_TOKEN: ${{ secrets.PIPEDRIVE_TOKEN }}
          EMAILJS_SERVICE_ID: ${{ secrets.EMAILJS_SERVICE_ID }}
          EMAILJS_TEMPLATE_ID: ${{ secrets.EMAILJS_TEMPLATE_ID }}
          EMAILJS_PUBLIC_KEY: ${{ secrets.EMAILJS_PUBLIC_KEY }}
          EMAILJS_PRIVATE_KEY: ${{ secrets.EMAILJS_PRIVATE_KEY }}
          TEAM_EMAILS: ${{ secrets.TEAM_EMAILS }}
          COMPANY_NAME: ${{ secrets.COMPANY_NAME }}
          ALERT_DAYS: ${{ secrets.ALERT_DAYS }}
        run: node auto-notify.js
